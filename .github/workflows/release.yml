name: Build & Release APK

on:
  push:
    tags:
      - 'v*' # 當你 push 以 v 開頭的標籤時觸發，例如 v0.2.0

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.x' # 使用最新的 3.x 版本
          channel: 'stable'

      - name: Get dependencies
        run: flutter pub get

      - name: Build APK (Obfuscated)
        run: flutter build apk --obfuscate --split-debug-info=build/app/outputs/symbols

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: build/app/outputs/flutter-apk/app-release.apk
          name: Release ${{ github.ref_name }}
          draft: true # 改為草稿模式，方便手動編輯敘述
          prerelease: false
          generate_release_notes: true # 自動產生更新日誌
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
